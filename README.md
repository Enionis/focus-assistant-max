# FocusHelper - Бот для Max с техникой Pomodoro

Бот для платформы Max, который помогает повысить продуктивность с помощью техники Pomodoro, умного планирования задач и AI-помощника.

## 🎯 Основные возможности

- 🍅 **Техника Pomodoro** - управление временем и фокусом
- 📝 **Управление задачами** - создание и отслеживание задач с подзадачами
- 🤖 **AI-помощник** - интеллектуальный помощник на базе OpenRouter API
- 📱 **Веб-приложение** - удобный интерфейс для работы с задачами
- ⏰ **Утренние напоминания** - автоматические напоминания о незавершенных задачах в 9:00
- 📊 **Статистика и прогресс** - отслеживание выполненных сессий, уровней и достижений

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Токен бота Max API
- API ключ OpenRouter (опционально, для AI-помощника)

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd xaxaton_max
   ```

2. **Установите зависимости:**
   ```bash
   cd bot
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения:**
   
   Создайте файл `.env` в корне проекта (рядом с папкой `bot`):
   ```env
   BOT_TOKEN=your_max_bot_token_here
   OPENROUTER_API_KEY=your_openrouter_api_key_here
   ```
   
   Или скопируйте шаблон:
   ```bash
   cp bot/env_template.txt .env
   ```
   
   Затем отредактируйте `.env` и добавьте свои токены.

4. **Запустите бота:**
   ```bash
   cd bot
   python main.py
   ```

### Запуск API для синхронизации (опционально)

Для синхронизации данных между веб-приложением и ботом:

```bash
cd bot
python run_sync_api.py
```

API сервер запустится на `http://localhost:8000`.

## 📱 Использование

### Команды бота

- `/start` - Начать работу с ботом, открыть главное меню
- `/help` или `/menu` - Показать меню со всеми командами
- `/test_reminder` - Тестовая команда для проверки утреннего напоминания

### Веб-приложение

Бот предоставляет веб-приложение для удобной работы с задачами. Откройте его через кнопку "📱 Открыть приложение" в боте.

**Возможности веб-приложения:**
- Создание и редактирование задач
- Разбивка задач на подзадачи
- Таймер Pomodoro с настраиваемой длительностью
- Статистика выполненных сессий
- Система уровней и достижений

### AI-помощник

Бот включает умного AI-помощника, который может:
- Отвечать на вопросы о продуктивности
- Помогать с планированием задач
- Объяснять технику Pomodoro
- Давать советы по повышению эффективности

Для использования AI-помощника нажмите кнопку "🤖 Умный помощник" в главном меню.

## 🏗️ Структура проекта

```
xaxaton_max/
├── bot/                   # Основной код бота
│   ├── main.py            # Точка входа, запуск бота
│   ├── router.py          # Обработчики команд и сообщений
│   ├── scheduler.py       # Планировщик утренних напоминаний
│   ├── states.py          # Состояния пользователей
│   ├── sync_api.py        # API для синхронизации данных
│   ├── run_sync_api.py    # Запуск API сервера
│   └── requirements.txt   # Зависимости Python
├── webapp/                # Веб-приложение
│   ├── index.html         # Главная страница
│   ├── app.js             # Логика приложения
│   ├── style.css          # Стили
│   └── manifest.json      # PWA манифест
├── env_template.txt       # Шаблон переменных окружения
├── Dockerfile             # Конфигурация Docker
└── README.md              # Этот файл
```

## 🔧 Разработка

### Локальная разработка

1. Установите зависимости (см. раздел "Установка")
2. Настройте `.env` файл с токенами
3. Запустите бота: `python bot/main.py`
4. Для тестирования синхронизации запустите API: `python bot/run_sync_api.py`

### Docker

Для запуска через Docker:

```bash
docker build -t focushelper .
docker run -d --env-file .env focushelper
```

## 🔑 Получение токенов

### BOT_TOKEN (обязательно)

Токен бота получается в панели разработчика Max API.

### OPENROUTER_API_KEY (опционально)

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Перейдите в настройки аккаунта
3. Создайте новый API ключ
4. Добавьте его в `.env` файл

**Примечание:** Без API ключа OpenRouter AI-помощник работать не будет, но остальной функционал бота будет доступен.


## 🛠️ Технологии

- **Python 3.11+** - основной язык бота
- **maxapi** - библиотека для работы с Max API
- **FastAPI** - API для синхронизации данных
- **OpenRouter API** - AI-модели для умного помощника
- **Vanilla JavaScript** - веб-приложение без фреймворков

## 📝 Лицензия

Open Source - используйте свободно!

## 🤝 Вклад

Приветствуются pull requests и issues!

## ⚠️ Важные замечания

1. **Файл `.env` не должен попадать в git** - добавьте его в `.gitignore`
2. **API для синхронизации** должен быть доступен из интернета для работы веб-приложения
3. **Утренние напоминания** отправляются автоматически в 9:00 по времени сервера

## 🐛 Решение проблем

### Бот не запускается

- Проверьте, что все зависимости установлены: `pip install -r bot/requirements.txt`
- Убедитесь, что файл `.env` создан и содержит правильные токены
- Проверьте логи на наличие ошибок

### AI-помощник не отвечает

- Проверьте, что `OPENROUTER_API_KEY` установлен в `.env`
- Убедитесь, что у вас есть доступ к интернету
- Проверьте баланс на OpenRouter (если требуется)

### Синхронизация не работает

- Убедитесь, что API сервер запущен: `python bot/run_sync_api.py`
- Проверьте, что URL API правильно настроен в `webapp/app.js`
- Проверьте логи API сервера на наличие ошибок
